pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        mavenLocal()
    }
}

rootProject.name = 'JSON+'

include(
        'core',
        'serialization',
        'files',
        'utilities',
        'kotlin'
)